export interface MenuItem {
  id: string
  label: string
  path: string
}

export interface MenuProps {
  items?: MenuItem[]
  onSearch?: (query: string) => void
}
export interface MenuItem {
  id: string
  label: string
  path: string
}

export interface MenuProps {
  items?: MenuItem[]
  onSearch?: (query: string) => void
}
